# 🎯 Proddy Bot - Персональный помощник продуктивности в MAX

Чат-бот для управления задачами, техники Pomodoro и отслеживания целей прямо в мессенджере MAX.

## 🚀 Возможности

- 🎯 **Pomodoro таймер** - 25 минут фокуса + 5 минут перерыва
- 📝 **Управление задачами** - добавление, выполнение, удаление задач
- 🎯 **Постановка целей** - отслеживание прогресса с визуализацией
- 📊 **Статистика** - аналитика продуктивности и выполненных сессий

## 🛠 Технологии

- **Backend**: Go 1.21+
- **Фреймворк**: MAX Bot API Client
- **Хранение**: In-memory storage
- **Контейнеризация**: Docker

## 📦 Быстрый старт

### Способ 1: Запуск через Docker (рекомендуется)

```bash
# Клонируй репозиторий
git clone https://github.com/your-username/proddy-bot.git
cd proddy-bot

# Собери Docker образ
docker build -t proddy-bot .

# Запусти контейнер
docker run -d -p 8080:8080 --name proddy-bot \
  -e BOT_TOKEN=your_bot_token_here \

### Способ 2: Локальный запуск
bash
# Клонируй репозиторий
git clone https://github.com/your-username/proddy-bot.git
cd proddy-bot

# Установи зависимости
go mod download

# Настрой переменные окружения
cp .env.example .env
# Отредактируй .env файл с твоим токеном

# Запусти приложение
go run ./cmd/bot

### ⚙️ Конфигурация
Создай файл .env в корне проекта:

BOT_TOKEN=your_max_bot_token_here

### Структура проекта

proddy-bot/
├── cmd/bot/
│   └── main.go              # Точка входа приложения
├── internal/
│   ├── handlers/
│   │   └── message_handler.go # Обработчики сообщений
│   ├── models/
│   │   ├── user.go          # Модель пользователя
│   │   ├── tasks.go         # Модель задач
│   │   └── pomodoro.go      # Модель Pomodoro сессий
│   ├── storage/
│   │   └── memory_storage.go # Система хранения данных
│   └── utils/
├── Dockerfile
├── go.mod
├── go.sum
└── README.md

### 🎮 Использование
После запуска бота в MAX:

Начни работу: напиши "начать"

Pomodoro: "старт помодоро", "перерыв", "стоп помодоро"

Задачи: "добавить задачу прочитать книгу", "список задач"

Цели: "добавить цель выучить английский", "список целей"

Статистика: "статистика" 

Помощь: "помощь"

### 🔧 Разработка
Сборка для разработки

go build -o bin/bot ./cmd/bot
./bin/bot

Запуск тестов
go test ./...


📊 API endpoints
POST /webhook - вебхук для получения сообщений от MAX
GET /health - проверка работоспособности сервиса

### 🐛 Troubleshooting
Проблема: Бот не отвечает на сообщения
Решение: Проверь корректность BOT_TOKEN и WEBHOOK_URL в .env файле

Проблема: Docker контейнер не запускается
Решение: Убедись что порт 8080 свободен или измени PORT в .env